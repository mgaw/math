Vorlesung: Analysis I*
Autor: Marius Gawrisch
Matrikelnummer: 545346
Zettel: Übungsblatt 6
Vorlage: zettel

**Aufgabe 1**

**(i)**

  (a_n)  und  (b_n)  sind konvergente Folgen,  limn a_n = a, \; limn b_n = b.  Zu zeigen:  (forall n ≥ 0 in NN : a_n ≤ b_n) => (a ≤ b).  

Beweisen mit Kontraposition. Nehmen an, dass  a > b.  Dann gibt es aber ein  N in NN  mit  a_N > b_N. qed  

Für die Folgen  (a_n)_{n in NN}  mit  a_n = 0  und  (b_n)_{n in NN}  mit  b_n = 1/n  gilt  a_n < b_n \; forall n in NN,  aber nicht  a < b,  da  a = b = 0.  

**(ii)**

  forall n in NN: a_n ≤ c_n ≤ b_n.  und  limn a_n = limn b_n =: c.  Zu zeigen ist, dass  (c_n)  gegen  c  konvergiert.

  limn a_n = c  heißt, dass  forall eps > 0 \;exists N in NN \;forall n ≥ N : |a_n-c| < eps.  Sei  eps > 0  beliebig gewählt. Dann  exists N in NN \;forall n ≥ N : |a_n - c| < eps  und  |b_n - c| < eps.  Da aber  forall n in NN : a_n ≤ c_n ≤ b_n  gilt damit auch  |c_n - c| < eps. qed  

pagebreak
header

**Aufgabe 2**

**(i)**

Betrachten  (x_n)  mit  limn x_n = x in RR.  und  X_n := \{x_k : k ≥ n\} sse RR.  Zu zeigen ist, dass  a_n := inf X_n  und  b_n := sup X_n  existieren und dass  limn a_n = limn b_n = x.  

Da  (x_n)  konvergiert liegen die Folgenglieder ab einem gewissen  N in NN  innerhalb von  triangle(x, eps). \;\{x_k : k ≥ N\}  ist also beschränkt. Ergänzt man diese Menge zu  X_n  bleibt sie aber beschränkt, da man höchstens endlich viele Element hinzufügen muss. Damit existieren Infimum und Supremum dieser Menge.

Sei  eps > 0  beliebig gewählt. Dann gibt es wegen der Konvergenz gegen  x  von  (x_n)  ein  N in NN,  ab dem alle weiteren Folgenglieder in  triangle(x, eps)  liegen. Anders ausgedrückt:  X_N sse triangle(x, eps).  Damit gilt auch  sup X_n in triangle(x, eps)  und  inf X_n in triangle(x, eps). qed  

**(ii)**

Betrachten Folge  (c_n)  mit  c_n := frac{1}{n+1}(sum^{2n}_{k=n} x_k).  Zu zeigen ist, dass  (c_n)  gegen  x  konvergiert.

Sei  eps > 0  beliebig gewählt, dann gilt wie oben  exists N in NN : X_N sse triangle(x, eps). \; c_N  ist aber der Durchschnitt von  N+1  Elementen aus  X_N,  damit gilt  c_N in triangle(x, eps). qed  

pagebreak
header

**Aufgabe 3**

**(i)**

  a_n = frac{n^2 + 2^n}{n + 3 cdot 2^n}, \;n≥0.  

  limn a_n = limn frac{n^2 + 2^n}{n + 3 cdot 2^n} = limn frac{frac{n^2}{2^n} + 1}{frac{n}{2^n} + 3} = frac{1}{3},  da  limn frac{n^2}{2^n} = limn frac{n}{2^n} = 0.  

**(ii)**

  b_n = frac{n^{n-2}}{n!}, \;n≥1  ist divergent, da der Zähler schnell wächst als der Nenner. Für das folgende Argument betrachten wir die letzten 3 Faktoren des Nenners  3 cdot 2 cdot 1  zusammen als ein Faktor, nämlich die 6. Es gibt dann ab  n = 7 \; (n-1)  Faktoren (nämlich alle außer dem ersten), die im Zähler größer sind als der an jeweils gleicher Stelle im Nenner. Diese Anzahl steigt mit steigendem  n,  daher ist die Folge divergent.

pagebreak
header

**Aufgabe 4**

**(i)**

Zu zeigen ist für alle  n in NN  und alle  z in CC  mit  z neq 1,  dass  sum_{k=0}^{n}z^k = frac{z^{n+1}-1}{z-1}  gilt.

Zeigen mit vollständiger Induktion.

IA:  n=0. \;z^0 = 1 = frac{z^1-1}{z-1}. \;checkmark  

IS:  (n) => (n+1). \; sum_{k=0}^{n+1}z^k = frac{z^{n+2}-1}{z-1} \;overset {IV.} <=>\; frac{z^{n+1}-1}{z-1} + z^{n+1} = frac{z^{n+2}-1}{z-1} \;<=>\; z^{n+1} - 1 + z^{n+2} - z^{n+1} = z^{n+2} - 1. qed  

**(ii)**

Fehlt.
